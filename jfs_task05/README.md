# Task-5 Java Advanced

## Programs Implemented

### Q1 – Convert Strings to Uppercase
- Uses `Stream.of()` and `map()` method
- Converts strings to uppercase
- Package: `uppercaseusingmap`

---

### Q2 – Empty String Check
- Checks whether strings are empty or not
- Prints only non-empty strings
- Uses Stream API
- Package: `filterstrings`

---

### Q3 – Student Gift Eligibility
- Stores student names using List interface
- Filters names starting with letter **'A'**
- Uses lambda expression and Stream API
- Package: `filterstudents`

---

### Q4 – Age Calculator
- Takes birthdate as user input
- Uses `java.time.LocalDate` and `Period`
- Calculates age in years, months, and days
- Package: `agecalculator`

---

